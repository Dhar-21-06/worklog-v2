"""
Shared Pydantic models.

These are DTOs used at the boundary between backend and desktop-agent (e.g.
the payload the agent posts when creating a report from the reminder
popup). Backend-internal-only schemas belong in backend/app/schemas instead.

Left empty in this bootstrap milestone - populated once the Report model
is introduced in Phase 1.
"""
